export const arrayConcat = (
  arr1: any[],
  arr2: any[],
) => arr1.concat(arr2);

export const arraySlice = (
  arr: any[],
  startIndex: number,
  stopIndex: number,
) => arr.slice(startIndex, stopIndex);
